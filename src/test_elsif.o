MODULE Elsif;
VAR x, y: INTEGER;
BEGIN
    x := 2;
    IF x = 1 THEN
        y := 10
    ELSIF x = 2 THEN
        y := 20
    ELSIF x = 3 THEN
        y := 30
    ELSE
        y := 0
    END;
    Out.Int(y, 2);
    Out.Ln
END Elsif.